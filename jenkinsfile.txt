pipeline {
  environment {
    registry = "boopathyvinoth/jenkinsdemo"
    registryCredential = 'docker'
    dockerImage = ''
  }
  agent any	
    stage('Building image') {
      steps{
        script {
          dockerImage = bat "docker-compose up --build -d"
        }
      }
    }
  }
}
